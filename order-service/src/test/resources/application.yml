spring:
  cloud:
    config:
      enabled: false   # Disable the config server
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      port: 6379
      host: localhost


eureka:
  client:
    enabled: false
